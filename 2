#include <iostream>
#include <math.h>
using namespace std;
int main()
{    
    const int n = 20;
    int N, B, C;
    cout << "Enter N, B, C: \n";
    cin >> N >> B >> C;
    int A[n];
    for (int i = 0; i < N; i++) {
        A[i] = B * pow(C, i);
        cout << A[i] << " ";
    }
    
}
